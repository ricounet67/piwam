<?php

/**
 * Base class that represents a row from the 'association' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.3.0-dev on:
 *
 * 04/20/09 13:59:08
 *
 * @package    lib.model.om
 */
abstract class BaseAssociation extends BaseObject  implements Persistent {


  const PEER = 'AssociationPeer';

	/**
	 * The Peer class.
	 * Instance provides a convenient way of calling static methods on a class
	 * that calling code may not be able to identify.
	 * @var        AssociationPeer
	 */
	protected static $peer;

	/**
	 * The value for the id field.
	 * @var        int
	 */
	protected $id;

	/**
	 * The value for the nom field.
	 * @var        string
	 */
	protected $nom;

	/**
	 * The value for the description field.
	 * @var        string
	 */
	protected $description;

	/**
	 * The value for the site_web field.
	 * @var        string
	 */
	protected $site_web;

	/**
	 * The value for the enregistre_par field.
	 * @var        int
	 */
	protected $enregistre_par;

	/**
	 * The value for the mis_a_jour_par field.
	 * @var        int
	 */
	protected $mis_a_jour_par;

	/**
	 * The value for the created_at field.
	 * @var        string
	 */
	protected $created_at;

	/**
	 * The value for the updated_at field.
	 * @var        string
	 */
	protected $updated_at;

	/**
	 * @var        Membre
	 */
	protected $aMembreRelatedByEnregistrePar;

	/**
	 * @var        Membre
	 */
	protected $aMembreRelatedByMisAJourPar;

	/**
	 * @var        array Activite[] Collection to store aggregation of Activite objects.
	 */
	protected $collActivites;

	/**
	 * @var        Criteria The criteria used to select the current contents of collActivites.
	 */
	private $lastActiviteCriteria = null;

	/**
	 * @var        array Compte[] Collection to store aggregation of Compte objects.
	 */
	protected $collComptes;

	/**
	 * @var        Criteria The criteria used to select the current contents of collComptes.
	 */
	private $lastCompteCriteria = null;

	/**
	 * @var        array CotisationType[] Collection to store aggregation of CotisationType objects.
	 */
	protected $collCotisationTypes;

	/**
	 * @var        Criteria The criteria used to select the current contents of collCotisationTypes.
	 */
	private $lastCotisationTypeCriteria = null;

	/**
	 * @var        array Depense[] Collection to store aggregation of Depense objects.
	 */
	protected $collDepenses;

	/**
	 * @var        Criteria The criteria used to select the current contents of collDepenses.
	 */
	private $lastDepenseCriteria = null;

	/**
	 * @var        array Membre[] Collection to store aggregation of Membre objects.
	 */
	protected $collMembres;

	/**
	 * @var        Criteria The criteria used to select the current contents of collMembres.
	 */
	private $lastMembreCriteria = null;

	/**
	 * @var        array Recette[] Collection to store aggregation of Recette objects.
	 */
	protected $collRecettes;

	/**
	 * @var        Criteria The criteria used to select the current contents of collRecettes.
	 */
	private $lastRecetteCriteria = null;

	/**
	 * @var        array Statut[] Collection to store aggregation of Statut objects.
	 */
	protected $collStatuts;

	/**
	 * @var        Criteria The criteria used to select the current contents of collStatuts.
	 */
	private $lastStatutCriteria = null;

	/**
	 * Flag to prevent endless save loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInSave = false;

	/**
	 * Flag to prevent endless validation loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInValidation = false;

	/**
	 * Initializes internal state of BaseAssociation object.
	 * @see        applyDefaults()
	 */
	public function __construct()
	{
		parent::__construct();
		$this->applyDefaultValues();
	}

	/**
	 * Applies default values to this object.
	 * This method should be called from the object's constructor (or
	 * equivalent initialization method).
	 * @see        __construct()
	 */
	public function applyDefaultValues()
	{
	}

	/**
	 * Get the [id] column value.
	 * 
	 * @return     int
	 */
	public function getId()
	{
		return $this->id;
	}

	/**
	 * Get the [nom] column value.
	 * 
	 * @return     string
	 */
	public function getNom()
	{
		return $this->nom;
	}

	/**
	 * Get the [description] column value.
	 * 
	 * @return     string
	 */
	public function getDescription()
	{
		return $this->description;
	}

	/**
	 * Get the [site_web] column value.
	 * 
	 * @return     string
	 */
	public function getSiteWeb()
	{
		return $this->site_web;
	}

	/**
	 * Get the [enregistre_par] column value.
	 * 
	 * @return     int
	 */
	public function getEnregistrePar()
	{
		return $this->enregistre_par;
	}

	/**
	 * Get the [mis_a_jour_par] column value.
	 * 
	 * @return     int
	 */
	public function getMisAJourPar()
	{
		return $this->mis_a_jour_par;
	}

	/**
	 * Get the [optionally formatted] temporal [created_at] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL, and 0 if column value is 0000-00-00 00:00:00
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getCreatedAt($format = 'Y-m-d H:i:s')
	{
		if ($this->created_at === null) {
			return null;
		}


		if ($this->created_at === '0000-00-00 00:00:00') {
			// while technically this is not a default value of NULL,
			// this seems to be closest in meaning.
			return null;
		} else {
			try {
				$dt = new DateTime($this->created_at);
			} catch (Exception $x) {
				throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->created_at, true), $x);
			}
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [optionally formatted] temporal [updated_at] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL, and 0 if column value is 0000-00-00 00:00:00
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getUpdatedAt($format = 'Y-m-d H:i:s')
	{
		if ($this->updated_at === null) {
			return null;
		}


		if ($this->updated_at === '0000-00-00 00:00:00') {
			// while technically this is not a default value of NULL,
			// this seems to be closest in meaning.
			return null;
		} else {
			try {
				$dt = new DateTime($this->updated_at);
			} catch (Exception $x) {
				throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->updated_at, true), $x);
			}
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Set the value of [id] column.
	 * 
	 * @param      int $v new value
	 * @return     Association The current object (for fluent API support)
	 */
	public function setId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->id !== $v) {
			$this->id = $v;
			$this->modifiedColumns[] = AssociationPeer::ID;
		}

		return $this;
	} // setId()

	/**
	 * Set the value of [nom] column.
	 * 
	 * @param      string $v new value
	 * @return     Association The current object (for fluent API support)
	 */
	public function setNom($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->nom !== $v) {
			$this->nom = $v;
			$this->modifiedColumns[] = AssociationPeer::NOM;
		}

		return $this;
	} // setNom()

	/**
	 * Set the value of [description] column.
	 * 
	 * @param      string $v new value
	 * @return     Association The current object (for fluent API support)
	 */
	public function setDescription($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->description !== $v) {
			$this->description = $v;
			$this->modifiedColumns[] = AssociationPeer::DESCRIPTION;
		}

		return $this;
	} // setDescription()

	/**
	 * Set the value of [site_web] column.
	 * 
	 * @param      string $v new value
	 * @return     Association The current object (for fluent API support)
	 */
	public function setSiteWeb($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->site_web !== $v) {
			$this->site_web = $v;
			$this->modifiedColumns[] = AssociationPeer::SITE_WEB;
		}

		return $this;
	} // setSiteWeb()

	/**
	 * Set the value of [enregistre_par] column.
	 * 
	 * @param      int $v new value
	 * @return     Association The current object (for fluent API support)
	 */
	public function setEnregistrePar($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->enregistre_par !== $v) {
			$this->enregistre_par = $v;
			$this->modifiedColumns[] = AssociationPeer::ENREGISTRE_PAR;
		}

		if ($this->aMembreRelatedByEnregistrePar !== null && $this->aMembreRelatedByEnregistrePar->getId() !== $v) {
			$this->aMembreRelatedByEnregistrePar = null;
		}

		return $this;
	} // setEnregistrePar()

	/**
	 * Set the value of [mis_a_jour_par] column.
	 * 
	 * @param      int $v new value
	 * @return     Association The current object (for fluent API support)
	 */
	public function setMisAJourPar($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->mis_a_jour_par !== $v) {
			$this->mis_a_jour_par = $v;
			$this->modifiedColumns[] = AssociationPeer::MIS_A_JOUR_PAR;
		}

		if ($this->aMembreRelatedByMisAJourPar !== null && $this->aMembreRelatedByMisAJourPar->getId() !== $v) {
			$this->aMembreRelatedByMisAJourPar = null;
		}

		return $this;
	} // setMisAJourPar()

	/**
	 * Sets the value of [created_at] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     Association The current object (for fluent API support)
	 */
	public function setCreatedAt($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->created_at !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->created_at !== null && $tmpDt = new DateTime($this->created_at)) ? $tmpDt->format('Y-m-d H:i:s') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d H:i:s') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->created_at = ($dt ? $dt->format('Y-m-d H:i:s') : null);
				$this->modifiedColumns[] = AssociationPeer::CREATED_AT;
			}
		} // if either are not null

		return $this;
	} // setCreatedAt()

	/**
	 * Sets the value of [updated_at] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     Association The current object (for fluent API support)
	 */
	public function setUpdatedAt($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->updated_at !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->updated_at !== null && $tmpDt = new DateTime($this->updated_at)) ? $tmpDt->format('Y-m-d H:i:s') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d H:i:s') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->updated_at = ($dt ? $dt->format('Y-m-d H:i:s') : null);
				$this->modifiedColumns[] = AssociationPeer::UPDATED_AT;
			}
		} // if either are not null

		return $this;
	} // setUpdatedAt()

	/**
	 * Indicates whether the columns in this object are only set to default values.
	 *
	 * This method can be used in conjunction with isModified() to indicate whether an object is both
	 * modified _and_ has some values set which are non-default.
	 *
	 * @return     boolean Whether the columns in this object are only been set with default values.
	 */
	public function hasOnlyDefaultValues()
	{
			// First, ensure that we don't have any columns that have been modified which aren't default columns.
			if (array_diff($this->modifiedColumns, array())) {
				return false;
			}

		// otherwise, everything was equal, so return TRUE
		return true;
	} // hasOnlyDefaultValues()

	/**
	 * Hydrates (populates) the object variables with values from the database resultset.
	 *
	 * An offset (0-based "start column") is specified so that objects can be hydrated
	 * with a subset of the columns in the resultset rows.  This is needed, for example,
	 * for results of JOIN queries where the resultset row includes columns from two or
	 * more tables.
	 *
	 * @param      array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
	 * @param      int $startcol 0-based offset column which indicates which restultset column to start with.
	 * @param      boolean $rehydrate Whether this object is being re-hydrated from the database.
	 * @return     int next starting column
	 * @throws     PropelException  - Any caught Exception will be rewrapped as a PropelException.
	 */
	public function hydrate($row, $startcol = 0, $rehydrate = false)
	{
		try {

			$this->id = ($row[$startcol + 0] !== null) ? (int) $row[$startcol + 0] : null;
			$this->nom = ($row[$startcol + 1] !== null) ? (string) $row[$startcol + 1] : null;
			$this->description = ($row[$startcol + 2] !== null) ? (string) $row[$startcol + 2] : null;
			$this->site_web = ($row[$startcol + 3] !== null) ? (string) $row[$startcol + 3] : null;
			$this->enregistre_par = ($row[$startcol + 4] !== null) ? (int) $row[$startcol + 4] : null;
			$this->mis_a_jour_par = ($row[$startcol + 5] !== null) ? (int) $row[$startcol + 5] : null;
			$this->created_at = ($row[$startcol + 6] !== null) ? (string) $row[$startcol + 6] : null;
			$this->updated_at = ($row[$startcol + 7] !== null) ? (string) $row[$startcol + 7] : null;
			$this->resetModified();

			$this->setNew(false);

			if ($rehydrate) {
				$this->ensureConsistency();
			}

			// FIXME - using NUM_COLUMNS may be clearer.
			return $startcol + 8; // 8 = AssociationPeer::NUM_COLUMNS - AssociationPeer::NUM_LAZY_LOAD_COLUMNS).

		} catch (Exception $e) {
			throw new PropelException("Error populating Association object", $e);
		}
	}

	/**
	 * Checks and repairs the internal consistency of the object.
	 *
	 * This method is executed after an already-instantiated object is re-hydrated
	 * from the database.  It exists to check any foreign keys to make sure that
	 * the objects related to the current object are correct based on foreign key.
	 *
	 * You can override this method in the stub class, but you should always invoke
	 * the base method from the overridden method (i.e. parent::ensureConsistency()),
	 * in case your model changes.
	 *
	 * @throws     PropelException
	 */
	public function ensureConsistency()
	{

		if ($this->aMembreRelatedByEnregistrePar !== null && $this->enregistre_par !== $this->aMembreRelatedByEnregistrePar->getId()) {
			$this->aMembreRelatedByEnregistrePar = null;
		}
		if ($this->aMembreRelatedByMisAJourPar !== null && $this->mis_a_jour_par !== $this->aMembreRelatedByMisAJourPar->getId()) {
			$this->aMembreRelatedByMisAJourPar = null;
		}
	} // ensureConsistency

	/**
	 * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
	 *
	 * This will only work if the object has been saved and has a valid primary key set.
	 *
	 * @param      boolean $deep (optional) Whether to also de-associated any related objects.
	 * @param      PropelPDO $con (optional) The PropelPDO connection to use.
	 * @return     void
	 * @throws     PropelException - if this object is deleted, unsaved or doesn't have pk match in db
	 */
	public function reload($deep = false, PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("Cannot reload a deleted object.");
		}

		if ($this->isNew()) {
			throw new PropelException("Cannot reload an unsaved object.");
		}

		if ($con === null) {
			$con = Propel::getConnection(AssociationPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		// We don't need to alter the object instance pool; we're just modifying this instance
		// already in the pool.

		$stmt = AssociationPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
		$row = $stmt->fetch(PDO::FETCH_NUM);
		$stmt->closeCursor();
		if (!$row) {
			throw new PropelException('Cannot find matching row in the database to reload object values.');
		}
		$this->hydrate($row, 0, true); // rehydrate

		if ($deep) {  // also de-associate any related objects?

			$this->aMembreRelatedByEnregistrePar = null;
			$this->aMembreRelatedByMisAJourPar = null;
			$this->collActivites = null;
			$this->lastActiviteCriteria = null;

			$this->collComptes = null;
			$this->lastCompteCriteria = null;

			$this->collCotisationTypes = null;
			$this->lastCotisationTypeCriteria = null;

			$this->collDepenses = null;
			$this->lastDepenseCriteria = null;

			$this->collMembres = null;
			$this->lastMembreCriteria = null;

			$this->collRecettes = null;
			$this->lastRecetteCriteria = null;

			$this->collStatuts = null;
			$this->lastStatutCriteria = null;

		} // if (deep)
	}

	/**
	 * Removes this object from datastore and sets delete attribute.
	 *
	 * @param      PropelPDO $con
	 * @return     void
	 * @throws     PropelException
	 * @see        BaseObject::setDeleted()
	 * @see        BaseObject::isDeleted()
	 */
	public function delete(PropelPDO $con = null)
	{

    foreach (sfMixer::getCallables('BaseAssociation:delete:pre') as $callable)
    {
      $ret = call_user_func($callable, $this, $con);
      if ($ret)
      {
        return;
      }
    }


		if ($this->isDeleted()) {
			throw new PropelException("This object has already been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(AssociationPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			AssociationPeer::doDelete($this, $con);
			$this->setDeleted(true);
			$con->commit();
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	

    foreach (sfMixer::getCallables('BaseAssociation:delete:post') as $callable)
    {
      call_user_func($callable, $this, $con);
    }

  }
	/**
	 * Persists this object to the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All modified related objects will also be persisted in the doSave()
	 * method.  This method wraps all precipitate database operations in a
	 * single transaction.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        doSave()
	 */
	public function save(PropelPDO $con = null)
	{

    foreach (sfMixer::getCallables('BaseAssociation:save:pre') as $callable)
    {
      $affectedRows = call_user_func($callable, $this, $con);
      if (is_int($affectedRows))
      {
        return $affectedRows;
      }
    }


    if ($this->isNew() && !$this->isColumnModified(AssociationPeer::CREATED_AT))
    {
      $this->setCreatedAt(time());
    }

    if ($this->isModified() && !$this->isColumnModified(AssociationPeer::UPDATED_AT))
    {
      $this->setUpdatedAt(time());
    }

		if ($this->isDeleted()) {
			throw new PropelException("You cannot save an object that has been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(AssociationPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			$affectedRows = $this->doSave($con);
			$con->commit();
    foreach (sfMixer::getCallables('BaseAssociation:save:post') as $callable)
    {
      call_user_func($callable, $this, $con, $affectedRows);
    }

			AssociationPeer::addInstanceToPool($this);
			return $affectedRows;
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Performs the work of inserting or updating the row in the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All related objects are also updated in this method.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        save()
	 */
	protected function doSave(PropelPDO $con)
	{
		$affectedRows = 0; // initialize var to track total num of affected rows
		if (!$this->alreadyInSave) {
			$this->alreadyInSave = true;

			// We call the save method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aMembreRelatedByEnregistrePar !== null) {
				if ($this->aMembreRelatedByEnregistrePar->isModified() || $this->aMembreRelatedByEnregistrePar->isNew()) {
					$affectedRows += $this->aMembreRelatedByEnregistrePar->save($con);
				}
				$this->setMembreRelatedByEnregistrePar($this->aMembreRelatedByEnregistrePar);
			}

			if ($this->aMembreRelatedByMisAJourPar !== null) {
				if ($this->aMembreRelatedByMisAJourPar->isModified() || $this->aMembreRelatedByMisAJourPar->isNew()) {
					$affectedRows += $this->aMembreRelatedByMisAJourPar->save($con);
				}
				$this->setMembreRelatedByMisAJourPar($this->aMembreRelatedByMisAJourPar);
			}

			if ($this->isNew() ) {
				$this->modifiedColumns[] = AssociationPeer::ID;
			}

			// If this object has been modified, then save it to the database.
			if ($this->isModified()) {
				if ($this->isNew()) {
					$pk = AssociationPeer::doInsert($this, $con);
					$affectedRows += 1; // we are assuming that there is only 1 row per doInsert() which
										 // should always be true here (even though technically
										 // BasePeer::doInsert() can insert multiple rows).

					$this->setId($pk);  //[IMV] update autoincrement primary key

					$this->setNew(false);
				} else {
					$affectedRows += AssociationPeer::doUpdate($this, $con);
				}

				$this->resetModified(); // [HL] After being saved an object is no longer 'modified'
			}

			if ($this->collActivites !== null) {
				foreach ($this->collActivites as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collComptes !== null) {
				foreach ($this->collComptes as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collCotisationTypes !== null) {
				foreach ($this->collCotisationTypes as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collDepenses !== null) {
				foreach ($this->collDepenses as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collMembres !== null) {
				foreach ($this->collMembres as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collRecettes !== null) {
				foreach ($this->collRecettes as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collStatuts !== null) {
				foreach ($this->collStatuts as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			$this->alreadyInSave = false;

		}
		return $affectedRows;
	} // doSave()

	/**
	 * Array of ValidationFailed objects.
	 * @var        array ValidationFailed[]
	 */
	protected $validationFailures = array();

	/**
	 * Gets any ValidationFailed objects that resulted from last call to validate().
	 *
	 *
	 * @return     array ValidationFailed[]
	 * @see        validate()
	 */
	public function getValidationFailures()
	{
		return $this->validationFailures;
	}

	/**
	 * Validates the objects modified field values and all objects related to this table.
	 *
	 * If $columns is either a column name or an array of column names
	 * only those columns are validated.
	 *
	 * @param      mixed $columns Column name or an array of column names.
	 * @return     boolean Whether all columns pass validation.
	 * @see        doValidate()
	 * @see        getValidationFailures()
	 */
	public function validate($columns = null)
	{
		$res = $this->doValidate($columns);
		if ($res === true) {
			$this->validationFailures = array();
			return true;
		} else {
			$this->validationFailures = $res;
			return false;
		}
	}

	/**
	 * This function performs the validation work for complex object models.
	 *
	 * In addition to checking the current object, all related objects will
	 * also be validated.  If all pass then <code>true</code> is returned; otherwise
	 * an aggreagated array of ValidationFailed objects will be returned.
	 *
	 * @param      array $columns Array of column names to validate.
	 * @return     mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
	 */
	protected function doValidate($columns = null)
	{
		if (!$this->alreadyInValidation) {
			$this->alreadyInValidation = true;
			$retval = null;

			$failureMap = array();


			// We call the validate method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aMembreRelatedByEnregistrePar !== null) {
				if (!$this->aMembreRelatedByEnregistrePar->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aMembreRelatedByEnregistrePar->getValidationFailures());
				}
			}

			if ($this->aMembreRelatedByMisAJourPar !== null) {
				if (!$this->aMembreRelatedByMisAJourPar->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aMembreRelatedByMisAJourPar->getValidationFailures());
				}
			}


			if (($retval = AssociationPeer::doValidate($this, $columns)) !== true) {
				$failureMap = array_merge($failureMap, $retval);
			}


				if ($this->collActivites !== null) {
					foreach ($this->collActivites as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collComptes !== null) {
					foreach ($this->collComptes as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collCotisationTypes !== null) {
					foreach ($this->collCotisationTypes as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collDepenses !== null) {
					foreach ($this->collDepenses as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collMembres !== null) {
					foreach ($this->collMembres as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collRecettes !== null) {
					foreach ($this->collRecettes as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collStatuts !== null) {
					foreach ($this->collStatuts as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}


			$this->alreadyInValidation = false;
		}

		return (!empty($failureMap) ? $failureMap : true);
	}

	/**
	 * Retrieves a field from the object by name passed in as a string.
	 *
	 * @param      string $name name
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     mixed Value of field.
	 */
	public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = AssociationPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		$field = $this->getByPosition($pos);
		return $field;
	}

	/**
	 * Retrieves a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @return     mixed Value of field at $pos
	 */
	public function getByPosition($pos)
	{
		switch($pos) {
			case 0:
				return $this->getId();
				break;
			case 1:
				return $this->getNom();
				break;
			case 2:
				return $this->getDescription();
				break;
			case 3:
				return $this->getSiteWeb();
				break;
			case 4:
				return $this->getEnregistrePar();
				break;
			case 5:
				return $this->getMisAJourPar();
				break;
			case 6:
				return $this->getCreatedAt();
				break;
			case 7:
				return $this->getUpdatedAt();
				break;
			default:
				return null;
				break;
		} // switch()
	}

	/**
	 * Exports the object as an array.
	 *
	 * You can specify the key type of the array by passing one of the class
	 * type constants.
	 *
	 * @param      string $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                        BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM. Defaults to BasePeer::TYPE_PHPNAME.
	 * @param      boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns.  Defaults to TRUE.
	 * @return     an associative array containing the field names (as keys) and field values
	 */
	public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true)
	{
		$keys = AssociationPeer::getFieldNames($keyType);
		$result = array(
			$keys[0] => $this->getId(),
			$keys[1] => $this->getNom(),
			$keys[2] => $this->getDescription(),
			$keys[3] => $this->getSiteWeb(),
			$keys[4] => $this->getEnregistrePar(),
			$keys[5] => $this->getMisAJourPar(),
			$keys[6] => $this->getCreatedAt(),
			$keys[7] => $this->getUpdatedAt(),
		);
		return $result;
	}

	/**
	 * Sets a field from the object by name passed in as a string.
	 *
	 * @param      string $name peer name
	 * @param      mixed $value field value
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     void
	 */
	public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = AssociationPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		return $this->setByPosition($pos, $value);
	}

	/**
	 * Sets a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @param      mixed $value field value
	 * @return     void
	 */
	public function setByPosition($pos, $value)
	{
		switch($pos) {
			case 0:
				$this->setId($value);
				break;
			case 1:
				$this->setNom($value);
				break;
			case 2:
				$this->setDescription($value);
				break;
			case 3:
				$this->setSiteWeb($value);
				break;
			case 4:
				$this->setEnregistrePar($value);
				break;
			case 5:
				$this->setMisAJourPar($value);
				break;
			case 6:
				$this->setCreatedAt($value);
				break;
			case 7:
				$this->setUpdatedAt($value);
				break;
		} // switch()
	}

	/**
	 * Populates the object using an array.
	 *
	 * This is particularly useful when populating an object from one of the
	 * request arrays (e.g. $_POST).  This method goes through the column
	 * names, checking to see whether a matching key exists in populated
	 * array. If so the setByName() method is called for that column.
	 *
	 * You can specify the key type of the array by additionally passing one
	 * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
	 * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
	 * The default key type is the column's phpname (e.g. 'AuthorId')
	 *
	 * @param      array  $arr     An array to populate the object from.
	 * @param      string $keyType The type of keys the array uses.
	 * @return     void
	 */
	public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
	{
		$keys = AssociationPeer::getFieldNames($keyType);

		if (array_key_exists($keys[0], $arr)) $this->setId($arr[$keys[0]]);
		if (array_key_exists($keys[1], $arr)) $this->setNom($arr[$keys[1]]);
		if (array_key_exists($keys[2], $arr)) $this->setDescription($arr[$keys[2]]);
		if (array_key_exists($keys[3], $arr)) $this->setSiteWeb($arr[$keys[3]]);
		if (array_key_exists($keys[4], $arr)) $this->setEnregistrePar($arr[$keys[4]]);
		if (array_key_exists($keys[5], $arr)) $this->setMisAJourPar($arr[$keys[5]]);
		if (array_key_exists($keys[6], $arr)) $this->setCreatedAt($arr[$keys[6]]);
		if (array_key_exists($keys[7], $arr)) $this->setUpdatedAt($arr[$keys[7]]);
	}

	/**
	 * Build a Criteria object containing the values of all modified columns in this object.
	 *
	 * @return     Criteria The Criteria object containing all modified values.
	 */
	public function buildCriteria()
	{
		$criteria = new Criteria(AssociationPeer::DATABASE_NAME);

		if ($this->isColumnModified(AssociationPeer::ID)) $criteria->add(AssociationPeer::ID, $this->id);
		if ($this->isColumnModified(AssociationPeer::NOM)) $criteria->add(AssociationPeer::NOM, $this->nom);
		if ($this->isColumnModified(AssociationPeer::DESCRIPTION)) $criteria->add(AssociationPeer::DESCRIPTION, $this->description);
		if ($this->isColumnModified(AssociationPeer::SITE_WEB)) $criteria->add(AssociationPeer::SITE_WEB, $this->site_web);
		if ($this->isColumnModified(AssociationPeer::ENREGISTRE_PAR)) $criteria->add(AssociationPeer::ENREGISTRE_PAR, $this->enregistre_par);
		if ($this->isColumnModified(AssociationPeer::MIS_A_JOUR_PAR)) $criteria->add(AssociationPeer::MIS_A_JOUR_PAR, $this->mis_a_jour_par);
		if ($this->isColumnModified(AssociationPeer::CREATED_AT)) $criteria->add(AssociationPeer::CREATED_AT, $this->created_at);
		if ($this->isColumnModified(AssociationPeer::UPDATED_AT)) $criteria->add(AssociationPeer::UPDATED_AT, $this->updated_at);

		return $criteria;
	}

	/**
	 * Builds a Criteria object containing the primary key for this object.
	 *
	 * Unlike buildCriteria() this method includes the primary key values regardless
	 * of whether or not they have been modified.
	 *
	 * @return     Criteria The Criteria object containing value(s) for primary key(s).
	 */
	public function buildPkeyCriteria()
	{
		$criteria = new Criteria(AssociationPeer::DATABASE_NAME);

		$criteria->add(AssociationPeer::ID, $this->id);

		return $criteria;
	}

	/**
	 * Returns the primary key for this object (row).
	 * @return     int
	 */
	public function getPrimaryKey()
	{
		return $this->getId();
	}

	/**
	 * Generic method to set the primary key (id column).
	 *
	 * @param      int $key Primary key.
	 * @return     void
	 */
	public function setPrimaryKey($key)
	{
		$this->setId($key);
	}

	/**
	 * Sets contents of passed object to values from current object.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      object $copyObj An object of Association (or compatible) type.
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @throws     PropelException
	 */
	public function copyInto($copyObj, $deepCopy = false)
	{

		$copyObj->setNom($this->nom);

		$copyObj->setDescription($this->description);

		$copyObj->setSiteWeb($this->site_web);

		$copyObj->setEnregistrePar($this->enregistre_par);

		$copyObj->setMisAJourPar($this->mis_a_jour_par);

		$copyObj->setCreatedAt($this->created_at);

		$copyObj->setUpdatedAt($this->updated_at);


		if ($deepCopy) {
			// important: temporarily setNew(false) because this affects the behavior of
			// the getter/setter methods for fkey referrer objects.
			$copyObj->setNew(false);

			foreach ($this->getActivites() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addActivite($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getComptes() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addCompte($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getCotisationTypes() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addCotisationType($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getDepenses() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addDepense($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getMembres() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addMembre($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getRecettes() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addRecette($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getStatuts() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addStatut($relObj->copy($deepCopy));
				}
			}

		} // if ($deepCopy)


		$copyObj->setNew(true);

		$copyObj->setId(NULL); // this is a auto-increment column, so set to default value

	}

	/**
	 * Makes a copy of this object that will be inserted as a new row in table when saved.
	 * It creates a new object filling in the simple attributes, but skipping any primary
	 * keys that are defined for the table.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @return     Association Clone of current object.
	 * @throws     PropelException
	 */
	public function copy($deepCopy = false)
	{
		// we use get_class(), because this might be a subclass
		$clazz = get_class($this);
		$copyObj = new $clazz();
		$this->copyInto($copyObj, $deepCopy);
		return $copyObj;
	}

	/**
	 * Returns a peer instance associated with this om.
	 *
	 * Since Peer classes are not to have any instance attributes, this method returns the
	 * same instance for all member of this class. The method could therefore
	 * be static, but this would prevent one from overriding the behavior.
	 *
	 * @return     AssociationPeer
	 */
	public function getPeer()
	{
		if (self::$peer === null) {
			self::$peer = new AssociationPeer();
		}
		return self::$peer;
	}

	/**
	 * Declares an association between this object and a Membre object.
	 *
	 * @param      Membre $v
	 * @return     Association The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setMembreRelatedByEnregistrePar(Membre $v = null)
	{
		if ($v === null) {
			$this->setEnregistrePar(NULL);
		} else {
			$this->setEnregistrePar($v->getId());
		}

		$this->aMembreRelatedByEnregistrePar = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the Membre object, it will not be re-added.
		if ($v !== null) {
			$v->addAssociationRelatedByEnregistrePar($this);
		}

		return $this;
	}


	/**
	 * Get the associated Membre object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     Membre The associated Membre object.
	 * @throws     PropelException
	 */
	public function getMembreRelatedByEnregistrePar(PropelPDO $con = null)
	{
		if ($this->aMembreRelatedByEnregistrePar === null && ($this->enregistre_par !== null)) {
			$c = new Criteria(MembrePeer::DATABASE_NAME);
			$c->add(MembrePeer::ID, $this->enregistre_par);
			$this->aMembreRelatedByEnregistrePar = MembrePeer::doSelectOne($c, $con);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aMembreRelatedByEnregistrePar->addAssociationsRelatedByEnregistrePar($this);
			 */
		}
		return $this->aMembreRelatedByEnregistrePar;
	}

	/**
	 * Declares an association between this object and a Membre object.
	 *
	 * @param      Membre $v
	 * @return     Association The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setMembreRelatedByMisAJourPar(Membre $v = null)
	{
		if ($v === null) {
			$this->setMisAJourPar(NULL);
		} else {
			$this->setMisAJourPar($v->getId());
		}

		$this->aMembreRelatedByMisAJourPar = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the Membre object, it will not be re-added.
		if ($v !== null) {
			$v->addAssociationRelatedByMisAJourPar($this);
		}

		return $this;
	}


	/**
	 * Get the associated Membre object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     Membre The associated Membre object.
	 * @throws     PropelException
	 */
	public function getMembreRelatedByMisAJourPar(PropelPDO $con = null)
	{
		if ($this->aMembreRelatedByMisAJourPar === null && ($this->mis_a_jour_par !== null)) {
			$c = new Criteria(MembrePeer::DATABASE_NAME);
			$c->add(MembrePeer::ID, $this->mis_a_jour_par);
			$this->aMembreRelatedByMisAJourPar = MembrePeer::doSelectOne($c, $con);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aMembreRelatedByMisAJourPar->addAssociationsRelatedByMisAJourPar($this);
			 */
		}
		return $this->aMembreRelatedByMisAJourPar;
	}

	/**
	 * Clears out the collActivites collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addActivites()
	 */
	public function clearActivites()
	{
		$this->collActivites = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collActivites collection (array).
	 *
	 * By default this just sets the collActivites collection to an empty array (like clearcollActivites());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initActivites()
	{
		$this->collActivites = array();
	}

	/**
	 * Gets an array of Activite objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Association has previously been saved, it will retrieve
	 * related Activites from storage. If this Association is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Activite[]
	 * @throws     PropelException
	 */
	public function getActivites($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AssociationPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collActivites === null) {
			if ($this->isNew()) {
			   $this->collActivites = array();
			} else {

				$criteria->add(ActivitePeer::ASSOCIATION_ID, $this->id);

				ActivitePeer::addSelectColumns($criteria);
				$this->collActivites = ActivitePeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(ActivitePeer::ASSOCIATION_ID, $this->id);

				ActivitePeer::addSelectColumns($criteria);
				if (!isset($this->lastActiviteCriteria) || !$this->lastActiviteCriteria->equals($criteria)) {
					$this->collActivites = ActivitePeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastActiviteCriteria = $criteria;
		return $this->collActivites;
	}

	/**
	 * Returns the number of related Activite objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Activite objects.
	 * @throws     PropelException
	 */
	public function countActivites(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AssociationPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collActivites === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(ActivitePeer::ASSOCIATION_ID, $this->id);

				$count = ActivitePeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(ActivitePeer::ASSOCIATION_ID, $this->id);

				if (!isset($this->lastActiviteCriteria) || !$this->lastActiviteCriteria->equals($criteria)) {
					$count = ActivitePeer::doCount($criteria, $con);
				} else {
					$count = count($this->collActivites);
				}
			} else {
				$count = count($this->collActivites);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Activite object to this object
	 * through the Activite foreign key attribute.
	 *
	 * @param      Activite $l Activite
	 * @return     void
	 * @throws     PropelException
	 */
	public function addActivite(Activite $l)
	{
		if ($this->collActivites === null) {
			$this->initActivites();
		}
		if (!in_array($l, $this->collActivites, true)) { // only add it if the **same** object is not already associated
			array_push($this->collActivites, $l);
			$l->setAssociation($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Association is new, it will return
	 * an empty collection; or if this Association has previously
	 * been saved, it will retrieve related Activites from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Association.
	 */
	public function getActivitesJoinMembreRelatedByEnregistrePar($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AssociationPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collActivites === null) {
			if ($this->isNew()) {
				$this->collActivites = array();
			} else {

				$criteria->add(ActivitePeer::ASSOCIATION_ID, $this->id);

				$this->collActivites = ActivitePeer::doSelectJoinMembreRelatedByEnregistrePar($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(ActivitePeer::ASSOCIATION_ID, $this->id);

			if (!isset($this->lastActiviteCriteria) || !$this->lastActiviteCriteria->equals($criteria)) {
				$this->collActivites = ActivitePeer::doSelectJoinMembreRelatedByEnregistrePar($criteria, $con, $join_behavior);
			}
		}
		$this->lastActiviteCriteria = $criteria;

		return $this->collActivites;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Association is new, it will return
	 * an empty collection; or if this Association has previously
	 * been saved, it will retrieve related Activites from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Association.
	 */
	public function getActivitesJoinMembreRelatedByMisAJourPar($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AssociationPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collActivites === null) {
			if ($this->isNew()) {
				$this->collActivites = array();
			} else {

				$criteria->add(ActivitePeer::ASSOCIATION_ID, $this->id);

				$this->collActivites = ActivitePeer::doSelectJoinMembreRelatedByMisAJourPar($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(ActivitePeer::ASSOCIATION_ID, $this->id);

			if (!isset($this->lastActiviteCriteria) || !$this->lastActiviteCriteria->equals($criteria)) {
				$this->collActivites = ActivitePeer::doSelectJoinMembreRelatedByMisAJourPar($criteria, $con, $join_behavior);
			}
		}
		$this->lastActiviteCriteria = $criteria;

		return $this->collActivites;
	}

	/**
	 * Clears out the collComptes collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addComptes()
	 */
	public function clearComptes()
	{
		$this->collComptes = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collComptes collection (array).
	 *
	 * By default this just sets the collComptes collection to an empty array (like clearcollComptes());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initComptes()
	{
		$this->collComptes = array();
	}

	/**
	 * Gets an array of Compte objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Association has previously been saved, it will retrieve
	 * related Comptes from storage. If this Association is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Compte[]
	 * @throws     PropelException
	 */
	public function getComptes($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AssociationPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collComptes === null) {
			if ($this->isNew()) {
			   $this->collComptes = array();
			} else {

				$criteria->add(ComptePeer::ASSOCIATION_ID, $this->id);

				ComptePeer::addSelectColumns($criteria);
				$this->collComptes = ComptePeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(ComptePeer::ASSOCIATION_ID, $this->id);

				ComptePeer::addSelectColumns($criteria);
				if (!isset($this->lastCompteCriteria) || !$this->lastCompteCriteria->equals($criteria)) {
					$this->collComptes = ComptePeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastCompteCriteria = $criteria;
		return $this->collComptes;
	}

	/**
	 * Returns the number of related Compte objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Compte objects.
	 * @throws     PropelException
	 */
	public function countComptes(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AssociationPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collComptes === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(ComptePeer::ASSOCIATION_ID, $this->id);

				$count = ComptePeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(ComptePeer::ASSOCIATION_ID, $this->id);

				if (!isset($this->lastCompteCriteria) || !$this->lastCompteCriteria->equals($criteria)) {
					$count = ComptePeer::doCount($criteria, $con);
				} else {
					$count = count($this->collComptes);
				}
			} else {
				$count = count($this->collComptes);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Compte object to this object
	 * through the Compte foreign key attribute.
	 *
	 * @param      Compte $l Compte
	 * @return     void
	 * @throws     PropelException
	 */
	public function addCompte(Compte $l)
	{
		if ($this->collComptes === null) {
			$this->initComptes();
		}
		if (!in_array($l, $this->collComptes, true)) { // only add it if the **same** object is not already associated
			array_push($this->collComptes, $l);
			$l->setAssociation($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Association is new, it will return
	 * an empty collection; or if this Association has previously
	 * been saved, it will retrieve related Comptes from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Association.
	 */
	public function getComptesJoinMembreRelatedByEnregistrePar($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AssociationPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collComptes === null) {
			if ($this->isNew()) {
				$this->collComptes = array();
			} else {

				$criteria->add(ComptePeer::ASSOCIATION_ID, $this->id);

				$this->collComptes = ComptePeer::doSelectJoinMembreRelatedByEnregistrePar($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(ComptePeer::ASSOCIATION_ID, $this->id);

			if (!isset($this->lastCompteCriteria) || !$this->lastCompteCriteria->equals($criteria)) {
				$this->collComptes = ComptePeer::doSelectJoinMembreRelatedByEnregistrePar($criteria, $con, $join_behavior);
			}
		}
		$this->lastCompteCriteria = $criteria;

		return $this->collComptes;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Association is new, it will return
	 * an empty collection; or if this Association has previously
	 * been saved, it will retrieve related Comptes from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Association.
	 */
	public function getComptesJoinMembreRelatedByMisAJourPar($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AssociationPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collComptes === null) {
			if ($this->isNew()) {
				$this->collComptes = array();
			} else {

				$criteria->add(ComptePeer::ASSOCIATION_ID, $this->id);

				$this->collComptes = ComptePeer::doSelectJoinMembreRelatedByMisAJourPar($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(ComptePeer::ASSOCIATION_ID, $this->id);

			if (!isset($this->lastCompteCriteria) || !$this->lastCompteCriteria->equals($criteria)) {
				$this->collComptes = ComptePeer::doSelectJoinMembreRelatedByMisAJourPar($criteria, $con, $join_behavior);
			}
		}
		$this->lastCompteCriteria = $criteria;

		return $this->collComptes;
	}

	/**
	 * Clears out the collCotisationTypes collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addCotisationTypes()
	 */
	public function clearCotisationTypes()
	{
		$this->collCotisationTypes = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collCotisationTypes collection (array).
	 *
	 * By default this just sets the collCotisationTypes collection to an empty array (like clearcollCotisationTypes());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initCotisationTypes()
	{
		$this->collCotisationTypes = array();
	}

	/**
	 * Gets an array of CotisationType objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Association has previously been saved, it will retrieve
	 * related CotisationTypes from storage. If this Association is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array CotisationType[]
	 * @throws     PropelException
	 */
	public function getCotisationTypes($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AssociationPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collCotisationTypes === null) {
			if ($this->isNew()) {
			   $this->collCotisationTypes = array();
			} else {

				$criteria->add(CotisationTypePeer::ASSOCIATION_ID, $this->id);

				CotisationTypePeer::addSelectColumns($criteria);
				$this->collCotisationTypes = CotisationTypePeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(CotisationTypePeer::ASSOCIATION_ID, $this->id);

				CotisationTypePeer::addSelectColumns($criteria);
				if (!isset($this->lastCotisationTypeCriteria) || !$this->lastCotisationTypeCriteria->equals($criteria)) {
					$this->collCotisationTypes = CotisationTypePeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastCotisationTypeCriteria = $criteria;
		return $this->collCotisationTypes;
	}

	/**
	 * Returns the number of related CotisationType objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related CotisationType objects.
	 * @throws     PropelException
	 */
	public function countCotisationTypes(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AssociationPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collCotisationTypes === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(CotisationTypePeer::ASSOCIATION_ID, $this->id);

				$count = CotisationTypePeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(CotisationTypePeer::ASSOCIATION_ID, $this->id);

				if (!isset($this->lastCotisationTypeCriteria) || !$this->lastCotisationTypeCriteria->equals($criteria)) {
					$count = CotisationTypePeer::doCount($criteria, $con);
				} else {
					$count = count($this->collCotisationTypes);
				}
			} else {
				$count = count($this->collCotisationTypes);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a CotisationType object to this object
	 * through the CotisationType foreign key attribute.
	 *
	 * @param      CotisationType $l CotisationType
	 * @return     void
	 * @throws     PropelException
	 */
	public function addCotisationType(CotisationType $l)
	{
		if ($this->collCotisationTypes === null) {
			$this->initCotisationTypes();
		}
		if (!in_array($l, $this->collCotisationTypes, true)) { // only add it if the **same** object is not already associated
			array_push($this->collCotisationTypes, $l);
			$l->setAssociation($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Association is new, it will return
	 * an empty collection; or if this Association has previously
	 * been saved, it will retrieve related CotisationTypes from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Association.
	 */
	public function getCotisationTypesJoinMembreRelatedByEnregistrePar($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AssociationPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collCotisationTypes === null) {
			if ($this->isNew()) {
				$this->collCotisationTypes = array();
			} else {

				$criteria->add(CotisationTypePeer::ASSOCIATION_ID, $this->id);

				$this->collCotisationTypes = CotisationTypePeer::doSelectJoinMembreRelatedByEnregistrePar($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(CotisationTypePeer::ASSOCIATION_ID, $this->id);

			if (!isset($this->lastCotisationTypeCriteria) || !$this->lastCotisationTypeCriteria->equals($criteria)) {
				$this->collCotisationTypes = CotisationTypePeer::doSelectJoinMembreRelatedByEnregistrePar($criteria, $con, $join_behavior);
			}
		}
		$this->lastCotisationTypeCriteria = $criteria;

		return $this->collCotisationTypes;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Association is new, it will return
	 * an empty collection; or if this Association has previously
	 * been saved, it will retrieve related CotisationTypes from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Association.
	 */
	public function getCotisationTypesJoinMembreRelatedByMisAJourPar($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AssociationPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collCotisationTypes === null) {
			if ($this->isNew()) {
				$this->collCotisationTypes = array();
			} else {

				$criteria->add(CotisationTypePeer::ASSOCIATION_ID, $this->id);

				$this->collCotisationTypes = CotisationTypePeer::doSelectJoinMembreRelatedByMisAJourPar($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(CotisationTypePeer::ASSOCIATION_ID, $this->id);

			if (!isset($this->lastCotisationTypeCriteria) || !$this->lastCotisationTypeCriteria->equals($criteria)) {
				$this->collCotisationTypes = CotisationTypePeer::doSelectJoinMembreRelatedByMisAJourPar($criteria, $con, $join_behavior);
			}
		}
		$this->lastCotisationTypeCriteria = $criteria;

		return $this->collCotisationTypes;
	}

	/**
	 * Clears out the collDepenses collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addDepenses()
	 */
	public function clearDepenses()
	{
		$this->collDepenses = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collDepenses collection (array).
	 *
	 * By default this just sets the collDepenses collection to an empty array (like clearcollDepenses());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initDepenses()
	{
		$this->collDepenses = array();
	}

	/**
	 * Gets an array of Depense objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Association has previously been saved, it will retrieve
	 * related Depenses from storage. If this Association is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Depense[]
	 * @throws     PropelException
	 */
	public function getDepenses($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AssociationPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collDepenses === null) {
			if ($this->isNew()) {
			   $this->collDepenses = array();
			} else {

				$criteria->add(DepensePeer::ASSOCIATION_ID, $this->id);

				DepensePeer::addSelectColumns($criteria);
				$this->collDepenses = DepensePeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(DepensePeer::ASSOCIATION_ID, $this->id);

				DepensePeer::addSelectColumns($criteria);
				if (!isset($this->lastDepenseCriteria) || !$this->lastDepenseCriteria->equals($criteria)) {
					$this->collDepenses = DepensePeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastDepenseCriteria = $criteria;
		return $this->collDepenses;
	}

	/**
	 * Returns the number of related Depense objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Depense objects.
	 * @throws     PropelException
	 */
	public function countDepenses(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AssociationPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collDepenses === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(DepensePeer::ASSOCIATION_ID, $this->id);

				$count = DepensePeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(DepensePeer::ASSOCIATION_ID, $this->id);

				if (!isset($this->lastDepenseCriteria) || !$this->lastDepenseCriteria->equals($criteria)) {
					$count = DepensePeer::doCount($criteria, $con);
				} else {
					$count = count($this->collDepenses);
				}
			} else {
				$count = count($this->collDepenses);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Depense object to this object
	 * through the Depense foreign key attribute.
	 *
	 * @param      Depense $l Depense
	 * @return     void
	 * @throws     PropelException
	 */
	public function addDepense(Depense $l)
	{
		if ($this->collDepenses === null) {
			$this->initDepenses();
		}
		if (!in_array($l, $this->collDepenses, true)) { // only add it if the **same** object is not already associated
			array_push($this->collDepenses, $l);
			$l->setAssociation($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Association is new, it will return
	 * an empty collection; or if this Association has previously
	 * been saved, it will retrieve related Depenses from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Association.
	 */
	public function getDepensesJoinCompte($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AssociationPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collDepenses === null) {
			if ($this->isNew()) {
				$this->collDepenses = array();
			} else {

				$criteria->add(DepensePeer::ASSOCIATION_ID, $this->id);

				$this->collDepenses = DepensePeer::doSelectJoinCompte($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(DepensePeer::ASSOCIATION_ID, $this->id);

			if (!isset($this->lastDepenseCriteria) || !$this->lastDepenseCriteria->equals($criteria)) {
				$this->collDepenses = DepensePeer::doSelectJoinCompte($criteria, $con, $join_behavior);
			}
		}
		$this->lastDepenseCriteria = $criteria;

		return $this->collDepenses;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Association is new, it will return
	 * an empty collection; or if this Association has previously
	 * been saved, it will retrieve related Depenses from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Association.
	 */
	public function getDepensesJoinActivite($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AssociationPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collDepenses === null) {
			if ($this->isNew()) {
				$this->collDepenses = array();
			} else {

				$criteria->add(DepensePeer::ASSOCIATION_ID, $this->id);

				$this->collDepenses = DepensePeer::doSelectJoinActivite($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(DepensePeer::ASSOCIATION_ID, $this->id);

			if (!isset($this->lastDepenseCriteria) || !$this->lastDepenseCriteria->equals($criteria)) {
				$this->collDepenses = DepensePeer::doSelectJoinActivite($criteria, $con, $join_behavior);
			}
		}
		$this->lastDepenseCriteria = $criteria;

		return $this->collDepenses;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Association is new, it will return
	 * an empty collection; or if this Association has previously
	 * been saved, it will retrieve related Depenses from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Association.
	 */
	public function getDepensesJoinMembreRelatedByEnregistrePar($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AssociationPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collDepenses === null) {
			if ($this->isNew()) {
				$this->collDepenses = array();
			} else {

				$criteria->add(DepensePeer::ASSOCIATION_ID, $this->id);

				$this->collDepenses = DepensePeer::doSelectJoinMembreRelatedByEnregistrePar($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(DepensePeer::ASSOCIATION_ID, $this->id);

			if (!isset($this->lastDepenseCriteria) || !$this->lastDepenseCriteria->equals($criteria)) {
				$this->collDepenses = DepensePeer::doSelectJoinMembreRelatedByEnregistrePar($criteria, $con, $join_behavior);
			}
		}
		$this->lastDepenseCriteria = $criteria;

		return $this->collDepenses;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Association is new, it will return
	 * an empty collection; or if this Association has previously
	 * been saved, it will retrieve related Depenses from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Association.
	 */
	public function getDepensesJoinMembreRelatedByMisAJourPar($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AssociationPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collDepenses === null) {
			if ($this->isNew()) {
				$this->collDepenses = array();
			} else {

				$criteria->add(DepensePeer::ASSOCIATION_ID, $this->id);

				$this->collDepenses = DepensePeer::doSelectJoinMembreRelatedByMisAJourPar($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(DepensePeer::ASSOCIATION_ID, $this->id);

			if (!isset($this->lastDepenseCriteria) || !$this->lastDepenseCriteria->equals($criteria)) {
				$this->collDepenses = DepensePeer::doSelectJoinMembreRelatedByMisAJourPar($criteria, $con, $join_behavior);
			}
		}
		$this->lastDepenseCriteria = $criteria;

		return $this->collDepenses;
	}

	/**
	 * Clears out the collMembres collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addMembres()
	 */
	public function clearMembres()
	{
		$this->collMembres = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collMembres collection (array).
	 *
	 * By default this just sets the collMembres collection to an empty array (like clearcollMembres());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initMembres()
	{
		$this->collMembres = array();
	}

	/**
	 * Gets an array of Membre objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Association has previously been saved, it will retrieve
	 * related Membres from storage. If this Association is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Membre[]
	 * @throws     PropelException
	 */
	public function getMembres($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AssociationPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collMembres === null) {
			if ($this->isNew()) {
			   $this->collMembres = array();
			} else {

				$criteria->add(MembrePeer::ASSOCIATION_ID, $this->id);

				MembrePeer::addSelectColumns($criteria);
				$this->collMembres = MembrePeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(MembrePeer::ASSOCIATION_ID, $this->id);

				MembrePeer::addSelectColumns($criteria);
				if (!isset($this->lastMembreCriteria) || !$this->lastMembreCriteria->equals($criteria)) {
					$this->collMembres = MembrePeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastMembreCriteria = $criteria;
		return $this->collMembres;
	}

	/**
	 * Returns the number of related Membre objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Membre objects.
	 * @throws     PropelException
	 */
	public function countMembres(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AssociationPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collMembres === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(MembrePeer::ASSOCIATION_ID, $this->id);

				$count = MembrePeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(MembrePeer::ASSOCIATION_ID, $this->id);

				if (!isset($this->lastMembreCriteria) || !$this->lastMembreCriteria->equals($criteria)) {
					$count = MembrePeer::doCount($criteria, $con);
				} else {
					$count = count($this->collMembres);
				}
			} else {
				$count = count($this->collMembres);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Membre object to this object
	 * through the Membre foreign key attribute.
	 *
	 * @param      Membre $l Membre
	 * @return     void
	 * @throws     PropelException
	 */
	public function addMembre(Membre $l)
	{
		if ($this->collMembres === null) {
			$this->initMembres();
		}
		if (!in_array($l, $this->collMembres, true)) { // only add it if the **same** object is not already associated
			array_push($this->collMembres, $l);
			$l->setAssociation($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Association is new, it will return
	 * an empty collection; or if this Association has previously
	 * been saved, it will retrieve related Membres from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Association.
	 */
	public function getMembresJoinStatut($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AssociationPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collMembres === null) {
			if ($this->isNew()) {
				$this->collMembres = array();
			} else {

				$criteria->add(MembrePeer::ASSOCIATION_ID, $this->id);

				$this->collMembres = MembrePeer::doSelectJoinStatut($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(MembrePeer::ASSOCIATION_ID, $this->id);

			if (!isset($this->lastMembreCriteria) || !$this->lastMembreCriteria->equals($criteria)) {
				$this->collMembres = MembrePeer::doSelectJoinStatut($criteria, $con, $join_behavior);
			}
		}
		$this->lastMembreCriteria = $criteria;

		return $this->collMembres;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Association is new, it will return
	 * an empty collection; or if this Association has previously
	 * been saved, it will retrieve related Membres from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Association.
	 */
	public function getMembresJoinMembreRelatedByEnregistrePar($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AssociationPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collMembres === null) {
			if ($this->isNew()) {
				$this->collMembres = array();
			} else {

				$criteria->add(MembrePeer::ASSOCIATION_ID, $this->id);

				$this->collMembres = MembrePeer::doSelectJoinMembreRelatedByEnregistrePar($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(MembrePeer::ASSOCIATION_ID, $this->id);

			if (!isset($this->lastMembreCriteria) || !$this->lastMembreCriteria->equals($criteria)) {
				$this->collMembres = MembrePeer::doSelectJoinMembreRelatedByEnregistrePar($criteria, $con, $join_behavior);
			}
		}
		$this->lastMembreCriteria = $criteria;

		return $this->collMembres;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Association is new, it will return
	 * an empty collection; or if this Association has previously
	 * been saved, it will retrieve related Membres from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Association.
	 */
	public function getMembresJoinMembreRelatedByMisAJourPar($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AssociationPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collMembres === null) {
			if ($this->isNew()) {
				$this->collMembres = array();
			} else {

				$criteria->add(MembrePeer::ASSOCIATION_ID, $this->id);

				$this->collMembres = MembrePeer::doSelectJoinMembreRelatedByMisAJourPar($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(MembrePeer::ASSOCIATION_ID, $this->id);

			if (!isset($this->lastMembreCriteria) || !$this->lastMembreCriteria->equals($criteria)) {
				$this->collMembres = MembrePeer::doSelectJoinMembreRelatedByMisAJourPar($criteria, $con, $join_behavior);
			}
		}
		$this->lastMembreCriteria = $criteria;

		return $this->collMembres;
	}

	/**
	 * Clears out the collRecettes collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addRecettes()
	 */
	public function clearRecettes()
	{
		$this->collRecettes = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collRecettes collection (array).
	 *
	 * By default this just sets the collRecettes collection to an empty array (like clearcollRecettes());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initRecettes()
	{
		$this->collRecettes = array();
	}

	/**
	 * Gets an array of Recette objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Association has previously been saved, it will retrieve
	 * related Recettes from storage. If this Association is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Recette[]
	 * @throws     PropelException
	 */
	public function getRecettes($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AssociationPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collRecettes === null) {
			if ($this->isNew()) {
			   $this->collRecettes = array();
			} else {

				$criteria->add(RecettePeer::ASSOCIATION_ID, $this->id);

				RecettePeer::addSelectColumns($criteria);
				$this->collRecettes = RecettePeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(RecettePeer::ASSOCIATION_ID, $this->id);

				RecettePeer::addSelectColumns($criteria);
				if (!isset($this->lastRecetteCriteria) || !$this->lastRecetteCriteria->equals($criteria)) {
					$this->collRecettes = RecettePeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastRecetteCriteria = $criteria;
		return $this->collRecettes;
	}

	/**
	 * Returns the number of related Recette objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Recette objects.
	 * @throws     PropelException
	 */
	public function countRecettes(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AssociationPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collRecettes === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(RecettePeer::ASSOCIATION_ID, $this->id);

				$count = RecettePeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(RecettePeer::ASSOCIATION_ID, $this->id);

				if (!isset($this->lastRecetteCriteria) || !$this->lastRecetteCriteria->equals($criteria)) {
					$count = RecettePeer::doCount($criteria, $con);
				} else {
					$count = count($this->collRecettes);
				}
			} else {
				$count = count($this->collRecettes);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Recette object to this object
	 * through the Recette foreign key attribute.
	 *
	 * @param      Recette $l Recette
	 * @return     void
	 * @throws     PropelException
	 */
	public function addRecette(Recette $l)
	{
		if ($this->collRecettes === null) {
			$this->initRecettes();
		}
		if (!in_array($l, $this->collRecettes, true)) { // only add it if the **same** object is not already associated
			array_push($this->collRecettes, $l);
			$l->setAssociation($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Association is new, it will return
	 * an empty collection; or if this Association has previously
	 * been saved, it will retrieve related Recettes from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Association.
	 */
	public function getRecettesJoinCompte($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AssociationPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collRecettes === null) {
			if ($this->isNew()) {
				$this->collRecettes = array();
			} else {

				$criteria->add(RecettePeer::ASSOCIATION_ID, $this->id);

				$this->collRecettes = RecettePeer::doSelectJoinCompte($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(RecettePeer::ASSOCIATION_ID, $this->id);

			if (!isset($this->lastRecetteCriteria) || !$this->lastRecetteCriteria->equals($criteria)) {
				$this->collRecettes = RecettePeer::doSelectJoinCompte($criteria, $con, $join_behavior);
			}
		}
		$this->lastRecetteCriteria = $criteria;

		return $this->collRecettes;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Association is new, it will return
	 * an empty collection; or if this Association has previously
	 * been saved, it will retrieve related Recettes from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Association.
	 */
	public function getRecettesJoinActivite($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AssociationPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collRecettes === null) {
			if ($this->isNew()) {
				$this->collRecettes = array();
			} else {

				$criteria->add(RecettePeer::ASSOCIATION_ID, $this->id);

				$this->collRecettes = RecettePeer::doSelectJoinActivite($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(RecettePeer::ASSOCIATION_ID, $this->id);

			if (!isset($this->lastRecetteCriteria) || !$this->lastRecetteCriteria->equals($criteria)) {
				$this->collRecettes = RecettePeer::doSelectJoinActivite($criteria, $con, $join_behavior);
			}
		}
		$this->lastRecetteCriteria = $criteria;

		return $this->collRecettes;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Association is new, it will return
	 * an empty collection; or if this Association has previously
	 * been saved, it will retrieve related Recettes from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Association.
	 */
	public function getRecettesJoinMembreRelatedByEnregistrePar($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AssociationPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collRecettes === null) {
			if ($this->isNew()) {
				$this->collRecettes = array();
			} else {

				$criteria->add(RecettePeer::ASSOCIATION_ID, $this->id);

				$this->collRecettes = RecettePeer::doSelectJoinMembreRelatedByEnregistrePar($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(RecettePeer::ASSOCIATION_ID, $this->id);

			if (!isset($this->lastRecetteCriteria) || !$this->lastRecetteCriteria->equals($criteria)) {
				$this->collRecettes = RecettePeer::doSelectJoinMembreRelatedByEnregistrePar($criteria, $con, $join_behavior);
			}
		}
		$this->lastRecetteCriteria = $criteria;

		return $this->collRecettes;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Association is new, it will return
	 * an empty collection; or if this Association has previously
	 * been saved, it will retrieve related Recettes from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Association.
	 */
	public function getRecettesJoinMembreRelatedByMisAJourPar($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AssociationPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collRecettes === null) {
			if ($this->isNew()) {
				$this->collRecettes = array();
			} else {

				$criteria->add(RecettePeer::ASSOCIATION_ID, $this->id);

				$this->collRecettes = RecettePeer::doSelectJoinMembreRelatedByMisAJourPar($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(RecettePeer::ASSOCIATION_ID, $this->id);

			if (!isset($this->lastRecetteCriteria) || !$this->lastRecetteCriteria->equals($criteria)) {
				$this->collRecettes = RecettePeer::doSelectJoinMembreRelatedByMisAJourPar($criteria, $con, $join_behavior);
			}
		}
		$this->lastRecetteCriteria = $criteria;

		return $this->collRecettes;
	}

	/**
	 * Clears out the collStatuts collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addStatuts()
	 */
	public function clearStatuts()
	{
		$this->collStatuts = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collStatuts collection (array).
	 *
	 * By default this just sets the collStatuts collection to an empty array (like clearcollStatuts());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initStatuts()
	{
		$this->collStatuts = array();
	}

	/**
	 * Gets an array of Statut objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Association has previously been saved, it will retrieve
	 * related Statuts from storage. If this Association is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Statut[]
	 * @throws     PropelException
	 */
	public function getStatuts($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AssociationPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collStatuts === null) {
			if ($this->isNew()) {
			   $this->collStatuts = array();
			} else {

				$criteria->add(StatutPeer::ASSOCIATION_ID, $this->id);

				StatutPeer::addSelectColumns($criteria);
				$this->collStatuts = StatutPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(StatutPeer::ASSOCIATION_ID, $this->id);

				StatutPeer::addSelectColumns($criteria);
				if (!isset($this->lastStatutCriteria) || !$this->lastStatutCriteria->equals($criteria)) {
					$this->collStatuts = StatutPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastStatutCriteria = $criteria;
		return $this->collStatuts;
	}

	/**
	 * Returns the number of related Statut objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Statut objects.
	 * @throws     PropelException
	 */
	public function countStatuts(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AssociationPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collStatuts === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(StatutPeer::ASSOCIATION_ID, $this->id);

				$count = StatutPeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(StatutPeer::ASSOCIATION_ID, $this->id);

				if (!isset($this->lastStatutCriteria) || !$this->lastStatutCriteria->equals($criteria)) {
					$count = StatutPeer::doCount($criteria, $con);
				} else {
					$count = count($this->collStatuts);
				}
			} else {
				$count = count($this->collStatuts);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Statut object to this object
	 * through the Statut foreign key attribute.
	 *
	 * @param      Statut $l Statut
	 * @return     void
	 * @throws     PropelException
	 */
	public function addStatut(Statut $l)
	{
		if ($this->collStatuts === null) {
			$this->initStatuts();
		}
		if (!in_array($l, $this->collStatuts, true)) { // only add it if the **same** object is not already associated
			array_push($this->collStatuts, $l);
			$l->setAssociation($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Association is new, it will return
	 * an empty collection; or if this Association has previously
	 * been saved, it will retrieve related Statuts from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Association.
	 */
	public function getStatutsJoinMembreRelatedByEnregistrePar($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AssociationPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collStatuts === null) {
			if ($this->isNew()) {
				$this->collStatuts = array();
			} else {

				$criteria->add(StatutPeer::ASSOCIATION_ID, $this->id);

				$this->collStatuts = StatutPeer::doSelectJoinMembreRelatedByEnregistrePar($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(StatutPeer::ASSOCIATION_ID, $this->id);

			if (!isset($this->lastStatutCriteria) || !$this->lastStatutCriteria->equals($criteria)) {
				$this->collStatuts = StatutPeer::doSelectJoinMembreRelatedByEnregistrePar($criteria, $con, $join_behavior);
			}
		}
		$this->lastStatutCriteria = $criteria;

		return $this->collStatuts;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Association is new, it will return
	 * an empty collection; or if this Association has previously
	 * been saved, it will retrieve related Statuts from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Association.
	 */
	public function getStatutsJoinMembreRelatedByMisAJourPar($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AssociationPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collStatuts === null) {
			if ($this->isNew()) {
				$this->collStatuts = array();
			} else {

				$criteria->add(StatutPeer::ASSOCIATION_ID, $this->id);

				$this->collStatuts = StatutPeer::doSelectJoinMembreRelatedByMisAJourPar($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(StatutPeer::ASSOCIATION_ID, $this->id);

			if (!isset($this->lastStatutCriteria) || !$this->lastStatutCriteria->equals($criteria)) {
				$this->collStatuts = StatutPeer::doSelectJoinMembreRelatedByMisAJourPar($criteria, $con, $join_behavior);
			}
		}
		$this->lastStatutCriteria = $criteria;

		return $this->collStatuts;
	}

	/**
	 * Resets all collections of referencing foreign keys.
	 *
	 * This method is a user-space workaround for PHP's inability to garbage collect objects
	 * with circular references.  This is currently necessary when using Propel in certain
	 * daemon or large-volumne/high-memory operations.
	 *
	 * @param      boolean $deep Whether to also clear the references on all associated objects.
	 */
	public function clearAllReferences($deep = false)
	{
		if ($deep) {
			if ($this->collActivites) {
				foreach ((array) $this->collActivites as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collComptes) {
				foreach ((array) $this->collComptes as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collCotisationTypes) {
				foreach ((array) $this->collCotisationTypes as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collDepenses) {
				foreach ((array) $this->collDepenses as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collMembres) {
				foreach ((array) $this->collMembres as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collRecettes) {
				foreach ((array) $this->collRecettes as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collStatuts) {
				foreach ((array) $this->collStatuts as $o) {
					$o->clearAllReferences($deep);
				}
			}
		} // if ($deep)

		$this->collActivites = null;
		$this->collComptes = null;
		$this->collCotisationTypes = null;
		$this->collDepenses = null;
		$this->collMembres = null;
		$this->collRecettes = null;
		$this->collStatuts = null;
			$this->aMembreRelatedByEnregistrePar = null;
			$this->aMembreRelatedByMisAJourPar = null;
	}


  public function __call($method, $arguments)
  {
    if (!$callable = sfMixer::getCallable('BaseAssociation:'.$method))
    {
      throw new sfException(sprintf('Call to undefined method BaseAssociation::%s', $method));
    }

    array_unshift($arguments, $this);

    return call_user_func_array($callable, $arguments);
  }


} // BaseAssociation
